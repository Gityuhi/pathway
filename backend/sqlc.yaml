version: "2"
sql:
  - engine: "postgresql"
    queries: "query/*.sql"
    schema: "../supabase/migrations/"
    gen:
      go:
        package: "db"
        out: "backend/db/sqlc"
        sql_package: "pgx/v5"