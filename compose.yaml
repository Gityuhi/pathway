services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: production
    container_name: pathway-api-prod
    ports:
      - ${API_PORT}:8080
    environment:
      - DB_SOURCE=${DB_SOURCE}
    restart: always


